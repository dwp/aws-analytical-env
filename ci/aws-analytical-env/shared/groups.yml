groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
      - preprod
      - production
  - name: pull-request
    jobs:
      - pull-request
      - pull-request_cognito
  - name: cognito
    jobs:
      - management-dev
      - management
  - name: taint-emr
    jobs:
      - taint-dev
      - taint-qa
      - taint-int
      - taint-preprod
      - taint-prod
  - name: codecommit
    jobs:
      - copy-rnd
      - copy-rnd-incoming
  - name: testing
    jobs:
      - testing-development
      - testing-qa
      - testing-production
      - metrics-submit-job-development
      - metrics-submit-job-prod

