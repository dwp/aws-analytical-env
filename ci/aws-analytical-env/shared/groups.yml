groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
  - name: pull-request
    jobs:
      - pull-request
      - pull-request_cognito
  - name: cognito
    jobs:
      - management-dev
      - management
